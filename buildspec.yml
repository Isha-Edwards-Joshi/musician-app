# DO NOT CHANGE VERSION
version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 8
  pre_build: 
    commands: 
  build:
    commands:
      -echo Build started on `date`
  post_build:
    commands: 
      -echo Build completed on `date`
